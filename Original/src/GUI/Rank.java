/*
 * package GUI;
 * 
 * import javax.swing.*; import java.awt.*;
 * 
 * public class Rank extends JFrame { private JProgressBar progressBar;
 * 
 * public Rank() { setTitle("유사한 디자인 프로그레스 바");
 * setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); setSize(300, 100);
 * setLayout(new BorderLayout());
 * 
 * // 프로그레스 바 생성 progressBar = new JProgressBar(); progressBar.setValue(50); //
 * 예시로 중간 값으로 설정 progressBar.setStringPainted(true);
 * progressBar.setForeground(Color.BLUE); // 전경색을 파란색으로 설정
 * progressBar.setBorder(BorderFactory.createLineBorder(Color.BLACK, 2)); // 테두리
 * 스타일 설정
 * 
 * add(progressBar, BorderLayout.CENTER);
 * 
 * setVisible(true); }
 * 
 * 
 * }
 */